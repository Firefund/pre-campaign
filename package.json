{
  "name": "pre-campaign",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "optimizeCSS": "purgecss --css assets/bundle.css --content page1/Page1.html --output page1/optimizedBundle.css -keyframes",
    "bundle": "mkdir --parents page1/assets && cp --recursive --update assets/fonts page1/assets && zip --junk-paths build/Page1 page1/*",
    "build": "npm run optimizeCSS && npm run bundle"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "purgecss": "^4.0.3"
  }
}
